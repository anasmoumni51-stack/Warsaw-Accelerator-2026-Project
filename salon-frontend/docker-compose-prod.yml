version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://salon-api.technical-task.live/v1}
    container_name: salon-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
